<h1>Categories</h1>
<ul>
<% @categories.each do |category| %>
  <h4>Name: <%= category.name %></h4>
  <ul>
    <li><%= link_to "Products", products_path(category_id: category.id) %></li>
  </ul>
<% end %>
</ul>
